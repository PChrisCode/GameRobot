class ICook:
    def Cook():
        raise NotImplementedError("Need to create an instance from an interface or an abstract class before calling methods on it.")
    def ServeCustomers():
        raise NotImplementedError("Need to create an instance from an interface or an abstract class before calling methods on it.")
    def CollectCoins():
        raise NotImplementedError("Need to create an instance from an interface or an abstract class before calling methods on it.")